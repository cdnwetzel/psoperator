"""Gatekeeper: the SEPARATE authority.

The runtime proposes; the gatekeeper disposes. Risk classification here is
deterministic pattern-matching — deliberately NOT done by the model, which
is the very component a hostile screen could manipulate.
"""
